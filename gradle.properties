#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8 -XX:+UseParallelGC -Dkotlin.daemon.jvm.options\="-Xmx4096M"
org.gradle.caching=true
org.gradle.configuration-cache=true

#Kotlin
kotlin.code.style=official
# Enable C Interop Commonization for Kotlin Multiplatform
kotlin.mpp.enableCInteropCommonization=true
kotlin.native.cacheKind=none

#Android
android.useAndroidX=true
android.nonTransitiveRClass=true

#SKIE
skie.analytics.disableUpload=true

# Publishing Configuration
# Note: Version is now automatically derived from Git tags via gradle-git-versioning plugin
# See build.gradle.kts for versioning configuration
#GROUP=org.idos
GROUP=io.github.vanderian
POM_ARTIFACT_ID=idos-sdk-kotlin

# Maven Central Publishing (configure in GitHub Secrets for CI)
# MAVEN_CENTRAL_USERNAME=
# MAVEN_CENTRAL_PASSWORD=
# SIGNING_KEY_ID=
# SIGNING_PASSWORD=
# SIGNING_KEY=

# KMMBridge GitHub Configuration (configure in GitHub Secrets for CI)
# GITHUB_PUBLISH_TOKEN=
# GITHUB_REPO=idos-network/idos-sdk-kotlin
